{
    "name": "zimeng-yan-cv",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d dist"
    },
    "devDependencies": {
        "@types/node": "^20.14.2",
        "gh-pages": "^6.3.0",
        "typescript": "^5.5.4",
        "vite": "^5.4.1"
    }
}
